# MNX Management Console v2.1.0 — pinned runtime dependencies
# packages/pip/ 디렉터리의 *.whl 파일과 버전을 일치시킨다.
# 오프라인 설치:
#   python3.12 -m pip install --no-index --find-links ./packages/pip -r requirements.txt

# TUI core
textual==8.2.1
rich==14.3.3
pygments==2.20.0
markdown-it-py==4.0.0
mdurl==0.1.2
mdit-py-plugins==0.5.0
linkify-it-py==2.1.0
uc-micro-py==2.0.0
platformdirs==4.9.4
typing_extensions==4.15.0

# HTTP / Elasticsearch monitor
requests==2.33.1
urllib3==2.6.3
certifi==2026.2.25
idna==3.11
charset-normalizer==3.4.6

# ⚠ 현재 코드에서 사용하지 않는 의존성 (2026-08-05 확인)
#   이 핀은 원래 "mnx_config Fernet 암호" 용이라고 적혀 있었으나, cryptography/Fernet
#   은 리포지토리 .py 전체에서 참조 0건이다. 실제 구현은 mnx_config.py:43-56 의
#   base64 인코딩(ENC: 접두어)이며 암호화가 아니다.
#   packages/pip/ 에 whl 이 없어 --no-index 설치가 이 핀 때문에 실패할 수 있다.
# ponytail: 핀을 지우면 "base64 = 암호화" 를 묵인하게 되므로 유지한다.
#           upgrade: 자격증명 보호 방침 결정 시 제거하거나 실제 암호화로 전환.
#           상세: docs/architecture/10_dependency.md §2.1 주의
cryptography>=43.0,<46
