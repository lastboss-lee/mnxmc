# MNX Management Console v2.1.0 — pinned runtime dependencies
# packages/pip/ 디렉터리의 *.whl 파일과 버전을 일치시킨다.
# 오프라인 설치:
#   python3.12 -m pip install --no-index --find-links ./packages/pip -r requirements.txt

# TUI core
textual==8.2.1
rich==14.3.3
pygments==2.20.0
markdown-it-py==4.0.0
mdurl==0.1.2
mdit-py-plugins==0.5.0
linkify-it-py==2.1.0
uc-micro-py==2.0.0
platformdirs==4.9.4
typing_extensions==4.15.0

# HTTP / Elasticsearch monitor
requests==2.33.1
urllib3==2.6.3
certifi==2026.2.25
idna==3.11
charset-normalizer==3.4.6

# Password encryption (mnx_config Fernet) — 시스템 패키지 의존
# (cryptography >=43.0 이 시스템 /usr/local/lib/python3.12 에 설치되어 있어야 함)
# apt: python3-cryptography  또는  pip install cryptography==43.0.1
cryptography>=43.0,<46
